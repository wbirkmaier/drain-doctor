# DrainDoctor Agent Notes

- Model drain behavior conservatively and call out incomplete scheduler assumptions.
- Never imply that an advisory patch can be applied automatically.
- Treat PodDisruptionBudget math and topology constraints as first-class evidence.
- Keep recommendations ordered and operationally readable.
